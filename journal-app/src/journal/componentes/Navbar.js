import { AppBar,Toolbar,IconButton } from "@mui/material"

export const Navbar = () => {
  return (
    <AppBar position="fixed">
        <Toolbar>
            <IconButton>
              
            </IconButton>
        </Toolbar>
    </AppBar>
  )
}
