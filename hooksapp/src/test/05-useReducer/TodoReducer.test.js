describe('pruebas en TodoReducer', () => {

    const initialState = [{
        id:1,
        todo:'terminar curso react fh',
        done:false
    }]

    test('debe de retornar el state inicial', () => {
        
    });
});